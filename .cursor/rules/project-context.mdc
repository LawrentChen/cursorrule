---
description: 文档入口、阅读顺序与修改前后对 docs 的责任
alwaysApply: true
---

使用本 rule 的项目，约定有三份开发文档：

1. `docs/BUSINESS.md` — 业务目标、领域对象、计算口径与公式、取样与归一化等业务规则、术语与页面能力
2. `docs/ARCHITECTURE.md` — 目录与分层、运行时与技术细节、数据流、配置与缓存路径；可含 Mermaid 图
3. `docs/API.md` — 项目对外可依赖的模块/函数契约：入参/出参语义、空数据约定等

**通读顺序**（首次进入或做大范围改动时）：BUSINESS → ARCHITECTURE → API。

**修改前 — 按需阅读**（不必每次三份全文通读）：

- 涉及业务口径、用户可见指标、文案与公式 → 先读 `BUSINESS.md`（及其中的外链小节，若有）
- 涉及目录结构、数据流、新页面/新包、缓存与配置、依赖方向 → 读 `ARCHITECTURE.md`
- 涉及对外暴露的函数签名、入参出参语义、被其它模块依赖的边界 → 读 `API.md`
- 明显局部修复（错别字、单一函数内部实现且**边界与口径不变**）→ 至少扫一眼与改动最相关的一份即可

**修改后 — 必须自检文档**：若行为、边界、路径或口径有变化，同步更新对应 `docs/` 文档；若不确定是否算「有变」，宁可补一句变更说明。提交前核对 BUSINESS / ARCHITECTURE / API 中与本次改动相关的段落是否仍与代码一致。

**冲突处理**：文档与代码不一致时，以代码为准先核实，再更新文档。

根目录 `README.md` 面向使用者（安装、运行、配置与数据源的简要说明）；深入开发以 `docs/` 三份为准。README 中关于运行环境的写法见 `env.mdc`。
