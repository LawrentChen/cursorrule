---
description: Agent 执行命令时的环境与 README 中环境说明的约定
alwaysApply: false
---

**Agent 在本仓库执行命令时**：默认先激活 conda 环境 ``，再运行（与使用者本机配置无关，仅约定助手侧执行方式）。

**根目录 `README.md`**：不要写死具体 conda/venv 名称或「必须使用某某环境」。各使用者配置不同，用一句话说明由使用者自行准备 Python 环境即可。
